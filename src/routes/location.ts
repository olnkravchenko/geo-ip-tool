import { FastifyPluginAsync } from 'fastify';

const locationRoutes: FastifyPluginAsync = async (app) => {
    // TODO: add GET endpoint for converting coords to IP
    // TODO: add GET endpoint to get all region IPs
    // TODO: add GET endpoint to get all country IPs
};

export default locationRoutes;
